{
  "name": "map-corrector",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node ./tools/build.mjs",
    "build:watch": "node ./tools/build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.23.1"
  }
}
